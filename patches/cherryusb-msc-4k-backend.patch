diff --git a/components/usb/cherryusb/class/msc/usbd_msc.c b/components/usb/cherryusb/class/msc/usbd_msc.c
index 4eb551c1..f8c87d30 100644
--- a/components/usb/cherryusb/class/msc/usbd_msc.c
+++ b/components/usb/cherryusb/class/msc/usbd_msc.c
@@ -14,6 +14,9 @@
 #define MSD_OUT_EP_IDX 0
 #define MSD_IN_EP_IDX  1
 
+/* Batch up to eight 512-byte sectors without shrinking the USB DMA buffer. */
+#define MSC_BACKEND_MAX_XFER 4096U
+
 /* Describe EndPoints configuration */
 static struct usbd_endpoint mass_ep_data[CONFIG_USBDEV_MAX_BUS][2];
 
@@ -604,7 +607,7 @@ static bool SCSI_write10(uint8_t busid, uint8_t **data, uint32_t *len)
         return false;
     }
     g_usbd_msc[busid].stage = MSC_DATA_OUT;
-    data_len = MIN(data_len, CONFIG_USBDEV_MSC_MAX_BUFSIZE);
+    data_len = MIN(data_len, MSC_BACKEND_MAX_XFER);
     usbd_ep_start_read(busid, mass_ep_data[busid][MSD_OUT_EP_IDX].ep_addr, g_usbd_msc[busid].block_buffer, data_len);
     return true;
 }
@@ -633,7 +636,7 @@ static bool SCSI_write12(uint8_t busid, uint8_t **data, uint32_t *len)
         return false;
     }
     g_usbd_msc[busid].stage = MSC_DATA_OUT;
-    data_len = MIN(data_len, CONFIG_USBDEV_MSC_MAX_BUFSIZE);
+    data_len = MIN(data_len, MSC_BACKEND_MAX_XFER);
     usbd_ep_start_read(busid, mass_ep_data[busid][MSD_OUT_EP_IDX].ep_addr, g_usbd_msc[busid].block_buffer, data_len);
     return true;
 }
@@ -644,7 +647,7 @@ static bool SCSI_processRead(uint8_t busid)
 
     USB_LOG_DBG("read lba:%d\r\n", g_usbd_msc[busid].start_sector);
 
-    transfer_len = MIN(g_usbd_msc[busid].nsectors * g_usbd_msc[busid].scsi_blk_size[g_usbd_msc[busid].cbw.bLUN], CONFIG_USBDEV_MSC_MAX_BUFSIZE);
+    transfer_len = MIN(g_usbd_msc[busid].nsectors * g_usbd_msc[busid].scsi_blk_size[g_usbd_msc[busid].cbw.bLUN], MSC_BACKEND_MAX_XFER);
 
     if (usbd_msc_sector_read(busid, g_usbd_msc[busid].cbw.bLUN, g_usbd_msc[busid].start_sector, g_usbd_msc[busid].block_buffer, transfer_len) != 0) {
         SCSI_SetSenseData(busid, SCSI_KCQHE_UREINRESERVEDAREA);
@@ -682,7 +685,7 @@ static bool SCSI_processWrite(uint8_t busid, uint32_t nbytes)
     if (g_usbd_msc[busid].nsectors == 0) {
         usbd_msc_send_csw(busid, CSW_STATUS_CMD_PASSED);
     } else {
-        data_len = MIN(g_usbd_msc[busid].nsectors * g_usbd_msc[busid].scsi_blk_size[g_usbd_msc[busid].cbw.bLUN], CONFIG_USBDEV_MSC_MAX_BUFSIZE);
+        data_len = MIN(g_usbd_msc[busid].nsectors * g_usbd_msc[busid].scsi_blk_size[g_usbd_msc[busid].cbw.bLUN], MSC_BACKEND_MAX_XFER);
         usbd_ep_start_read(busid, mass_ep_data[busid][MSD_OUT_EP_IDX].ep_addr, g_usbd_msc[busid].block_buffer, data_len);
     }
 
